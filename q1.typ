#let thf = $hat(tilde(f))$
#let tf = $tilde(f)$

It is easy to see that for $0 <= n < L$, we have $ f_n = (tf_n + tf_(-n-1))/2.
$

We can write $tf_n$ as it's inverse fourier transform :

$ tf_n &= 1/(2L) sum_(k=0)^(2L-1) thf_k e^(2 i k pi n / (2L)) \ & =  $





